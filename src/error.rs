use std::fmt;
use std::io;

#[derive(Debug)]
}

    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        match self {
        }
    }
}

    fn from(error: io::Error) -> Self {
    }
}

